#!/usr/bin/env bash

set -e

for input in $(ls -1 data/*.txt); do
  name=$(basename $input)
  output=data/json/"${name%.txt}.json"
  jq -R -s -c 'split("\n")' < $input > $output
done

npm publish --access=public
