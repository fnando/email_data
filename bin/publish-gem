#!/usr/bin/env bash

set -e

gem install bundler
bundle update

rake release

terminal-notifier \
  -message "Just published a new gem version." \
  -ignoreDnD \
  -title "email_data" \
  -group homebrew \
  &> /dev/null
